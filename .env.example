# No API keys. No external AI services. No secrets should be committed.
#
# Backend values live in backend/.env, based on backend/.env.example.
# Frontend values live in frontend/.env, based on frontend/.env.example.
